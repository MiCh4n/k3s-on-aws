apiVersion: v1
kind: Pod
metadata:
  labels:
    name: terraform
spec:
  containers:
    - name: terraform
      image: hashicorp/terraform:0.12.21
      command:
        - cat
      tty: true
      resources:
        limits:
          memory: "512Mi"
          cpu: "350m"